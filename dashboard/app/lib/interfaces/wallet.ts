export type TWallet = {
  _id: string;
  accountId: string;
  firstName: string;
  lastName: string;
  balance: number;
  email: string;
  currency: string;
  createdAt: string;
  updatedAt: string;
};
